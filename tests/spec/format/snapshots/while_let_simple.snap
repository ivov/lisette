---
source: tests/spec/format/mod.rs
description: "input: fn test(opt: Option<int>) { while let Some(x) = opt { use(x); } }"
---
fn test(opt: Option<int>) {
  while let Some(x) = opt {
    use(x)
  }
}
