---
source: tests/spec/build/mod.rs
---
// === main.go ===
package main

import (
	"bufio"
	"fmt"
	"strings"
)

func main() {
	reader := bufio.NewReader(strings.NewReader("hello\nworld"))
	b, err := reader.ReadByte()
	c := b
	if err == nil {
		fmt.Print(c)
	} else {
		fmt.Print("error")
	}
}
