---
source: tests/spec/build/mod.rs
---
// === main.go ===
package main

import (
	"fmt"
	"io"
	"strings"
)

func main() {
	reader := strings.NewReader("hello")
	ret_1, err := io.ReadAll(reader)
	if err == nil {
		fmt.Print("ok")
	} else if ret_1 == nil {
		fmt.Print("err")
	} else {
		fmt.Print("both")
	}
}
