---
source: tests/spec/build/mod.rs
---
// === main.go ===
package main

import (
	"fmt"
	"sync"
)

func main() {
	m := sync.Map{}
	m.Store("key", "value")
	if v, ok := m.Load("key"); ok {
		fmt.Printf("got: %v\n", v)
	} else {
		fmt.Println("not found")
	}
}
